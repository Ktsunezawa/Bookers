<h1>Books</h1>
<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Body</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <% @books.each do |book| %>
    <tbody>
        <tr>
            <td><%= book.title %></td>
            <td><%= book.body %></td>
            <td><%= link_to "Show",  book_path(book) %></td>
            <td><%= link_to "Edit", edit_book_path(book) %></td>
            <td><%= link_to "Destroy",destroy_book_path(book.id), method: :delete, "deta-confirm" => "Are you sure?" %></td>
        </tr>
    </tbody>
    <% end %>
</table>

<h2>New book</h2>
<%= form_with model:@book, local:true do |f| %> 
<div class="title">
<div class="title-name">Title</div>
<%= f.text_field :title %>
</div>
<div class="body">
<div class="body-content">Body</div>
<%= f.text_area :body %>
</div>
<div class="create">
<%= f.submit "Create Book" %>
</div>

<% end %>